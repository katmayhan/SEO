{
  "name": "baboodle-seo-dashboard",
  "version": "1.0.0",
  "description": "SEO & AEO dashboard for baboodle.co.uk — tracks rankings, actions, and AI search visibility",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'Static site — no build step required'",
    "sync-gsc": "netlify functions:invoke gsc-sync",
    "measure": "netlify functions:invoke measure-impact"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}
